# MW-V12-09 · Cut the new Mellowa candidate and replace the superseded verdict

- **Run after:** MW-V12-01 through MW-V12-08 merged
- **Primary risk removed:** The current release record does not certify the current product code.

## Prompt

Create the new Mellowa release candidate after every intended code change is merged. Do not add product features in this prompt.

Procedure:
1. Confirm clean working tree and exact HEAD.
2. Prove the old RC is superseded and preserve its evidence as history.
3. Freeze the new candidate SHA.
4. Run at that exact SHA:
   - lint;
   - strict typecheck;
   - complete unit/contract/safety suite;
   - eval gate;
   - production build;
   - public E2E at all configured viewports;
   - authenticated journeys for every fixture;
   - complete daily-state matrix;
   - performance suite with honest lab labels;
   - release-check with authorized production presence data.
5. Record exact counts and raw artifacts. Required skips make the candidate conditional or blocked, never green.
6. Verify live EUR price objects through the read-only script.
7. Reconcile owner evidence without fabricating completion:
   - real €9.99 capture-to-refund;
   - duplicate-cron and forced-provider-failure observations;
   - key rotation and isolated restore.
8. Update manifest, launch-go-no-go and README from one machine-readable source.
9. Remove supersededNote only when the new candidate and its evidence are valid.
10. Compute:
    - capped private beta verdict;
    - unrestricted public paid verdict;
    - expansion verdict based on beta data.

Verdict definitions:
- GO: all required P0/P1 conditions actually passed with candidate-pinned or live evidence.
- CONDITIONAL GO: only explicitly accepted required risks remain.
- NO-GO: unaccepted blockers, stale evidence, required skips or candidate drift.

Finish with one authoritative status document, exact remaining owner actions, rollback triggers and a statement of what has not been proven.
