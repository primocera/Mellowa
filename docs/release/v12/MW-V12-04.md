# MW-V12-04 · Finish reminder, cron and email failure-path rehearsal

- **Run after:** MW-V12-03
- **Primary risk removed:** Five of seven live reminder checks are evidenced; duplicate-cron dedupe and forced provider failure/retry remain unobserved.

> Note: uncommitted trial-reminder/email work from a prior session was committed at the start of `launch/v12` — review it as part of this prompt's inspection step.

## Prompt

Complete the engineering side of Mellowa reminder/email reliability and prepare the final two owner-run observations. Do not intentionally break the live provider yourself.

Inspect:
- reminder cron routes and scheduling docs
- event-key/deduplication storage
- retry/backoff/dead-letter behavior
- src/lib/email/send.ts and lifecycle templates
- one-click unsubscribe route and wellbeing_profiles consent state
- transactional versus optional mail classification
- docs/ops-cron.md and current rehearsal evidence

Required implementation:
1. Add deterministic tests for two identical cron invocations while the account remains eligible on both runs. Prove the dedupe key, not unsubscribe or empty scanning, prevents the second send.
2. Add provider-failure simulations covering transient failure, retry/backoff, final success and permanent failure/dead-letter.
3. Persist provider failure reason safely without storing message body, health input or secrets.
4. Optional reminders must stop at query level after unsubscribe.
5. Billing, security, deletion and other transactional mail must remain enabled after optional-mail unsubscribe.
6. The settings surface must clearly reflect "off because you unsubscribed" and provide a safe explicit re-enable flow if supported.
7. Validate timezone windows, quiet hours, DST boundaries and wrapping windows with deterministic fixtures.
8. Keep the corrected sender-address sanitizer and ensure malformed angle-bracket-only input fails safely.
9. Update the owner worksheet for:
   - a duplicate eligible cron invocation;
   - a deliberate provider failure in a safe test account;
   - expected logs, retry transitions, evidence and cleanup.

Do not claim live rehearsal success from mocks. Keep the manifest item open until owner evidence exists. Run email, consent, cron, privacy, typecheck and build checks.
