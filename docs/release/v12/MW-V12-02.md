# MW-V12-02 · Run every authenticated and daily state without silent skips

- **Run after:** MW-V12-01
- **Primary risk removed:** Earlier suites looked green while required journeys skipped or fixtures could not produce required states.

## Prompt

Make Mellowa's authenticated browser evidence complete, deterministic and candidate-pinned.

Inspect:
- playwright.config.ts
- e2e/journeys.spec.ts
- e2e/daily-journey.spec.ts
- e2e/support/harness.ts
- scripts/seed-test-user.mjs
- CI authenticated-e2e job and RC_GATE behavior
- current raw evidence conventions

Requirements:
1. Enumerate every required user state and journey in one canonical test matrix.
2. Include trial-eligible, trial-used, trialing, active, past_due, canceled, sample-used and no-subscription states where the product supports them.
3. Each fixture must be constructible, isolated and validated before the test begins.
4. The release runner must execute every required matrix row. A zero-test run, missing env or required skip must be BLOCKED/non-zero in RC mode.
5. Ordinary PR CI may omit secret-dependent execution, but must display NOT RUN and must never produce release evidence.
6. Cover desktop, 375px and 320px, plus keyboard-critical paths.
7. Cover:
   - signup confirmation/callback;
   - login/sign out;
   - onboarding;
   - free sample;
   - pricing and trial eligibility;
   - checkout return and entitlement;
   - daily check-in;
   - plan generation;
   - low-capacity day;
   - adjust remaining items;
   - completed-item preservation;
   - undo;
   - weekly carry-forward;
   - settings, consent and deletion;
   - provider error and session expiry.
8. Replace timing-sensitive one-shot text reads with state-aware assertions. Do not hide real flakes with retries or blanket timeout increases.
9. Capture raw artifacts with exact SHA, fixture, viewport and pass/fail/skip counts.
10. Add integrity tests proving required tests cannot silently become unreachable after copy or selector changes.

Do not seed or mutate production. If a non-production environment is unavailable, finish the harness changes, leave the release item unresolved and provide the exact owner execution commands.
