# MW-V12-01 · Reconcile current HEAD with the superseded release candidate

- **Run after:** First Mellowa prompt (start here)
- **Primary risk removed:** The manifest is pinned to RC 0025a502 while product code changed afterward; the manifest itself says the candidate is superseded.

## Prompt

You are working in primocera/Mellowa on the latest default branch. Reconcile release truth before making additional product changes.

Verified problem to confirm:
- docs/release/manifest.v11.json is pinned to RC 0025a502.
- Current main contains later product changes including mobile sign-out, paywall hydration, EUR billing contracts, checkout/webhook/auth/email/unsubscribe fixes.
- The manifest contains a supersededNote stating that the candidate must be re-cut and gates re-run.
- Existing CONDITIONAL GO text must not be read as a verdict for the current HEAD.

Required implementation:
1. Inspect AGENTS.md and current Next.js repository documentation relevant to files touched.
2. Compare the old RC to current HEAD and create a machine-readable changed-file classification.
3. Make the canonical release surface visibly show:
   - previous RC = SUPERSEDED;
   - current HEAD = NOT YET FROZEN;
   - current launch verdict = NO-GO / UNASSESSED until a new candidate is cut.
4. Ensure release-manifest validation fails if:
   - supersededNote is present while a verdict is presented as active;
   - rcSha differs from product-code HEAD without an allowed documentation-only classification;
   - evidence SHA differs from the active candidate;
   - a current document omits the superseded state.
5. Keep historical evidence intact. Do not rewrite old evidence files to pretend they were produced at current HEAD.
6. Update README and launch-go-no-go text so nobody can quote the old CONDITIONAL GO as current.
7. Add a clear candidate lifecycle: draft/unfrozen -> frozen -> superseded.
8. Do not cut the new candidate yet. Later prompts still change product and test code.

Run release-manifest tests and report the exact product files that invalidated RC 0025a502.
